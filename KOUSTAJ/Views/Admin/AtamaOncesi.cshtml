@model IEnumerable<projeb.Models.Entity.ogretmen>

@{
    Layout = "~/Views/Shared/_AdminTabLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="@Url.Content("~/Content/bootstrap.css")" rel="stylesheet" type="text/css" />
    <link href="@Url.Content("~/Content/vendor/boostrap/bootstrap.js")" rel="stylesheet" type="text/css" />

    <title>Ogrenci Ekle</title>
</head>
@if (Session["admin"] == null)
{
    <script>
        window.alert("Sayfayı görüntüleyebilmeniz için giriş yapmanız gerekmektedir.");
        window.location.href = 'https://localhost:44396/Admin/Login';
    </script>

}
else
{
    <body>
        <br />
        @using (Html.BeginForm("AtamaOncesi", "Admin", FormMethod.Post))
        {
            <div>

                <form>

                    <div class="form-group">
                        @if (@ViewBag.m != null)
                        {@ViewBag.m}
                        <label for="exampleInputBolum1">Atama Yapmak İstediğiniz Öğretim Görevlisini Seçiniz</label><br />
                        <select name="bolum" id="exampleInputBolum1" class="form-control">
                            @foreach (var item in Model)
                            {
                                <option value=@item.id>@item.adsoyad</option>
                            }
                        </select>
                    </div>

                    <button type="submit" class="btn btn-primary">Devam Et -></button>
                </form>

            </div>
        }
    </body>
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />


}
</html>